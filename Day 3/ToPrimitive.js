// To Primitive 
// This function takes an input argument and converts it into a non object tyle(primitive type)

// if it can't convert it will throw error it takes One more optional parameter called as prefered type 
// if we have two or more than two values that can become answer this optional argument helps us.


/**
 * If prefered type is not given hint = "default" => this is new veriable we initialise 
 * else if prefered type is string , hint = "String", this hint is new veriable we initialise
 * else if prefered type is number , hint = "Number", this hint is new veriable we initialise
 * if hint = 'default' then hint = "Number"
 */